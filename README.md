# installation
```bash
clone https://github.com/hystking/vim-setting ~/.vim
echo "call pathogen#infect()" > ~/.vimrc
cd ~/.vim
git submodule init
git submodule update
```

# updating
```
git submodule update
```
